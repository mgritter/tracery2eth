# -*- Mode: python -*-

env = Environment()

# Not used in current prototype
#env.Command( "lib/solidity-stringutils",
#             None,
#"git clone https://github.com/Arachnid/solidity-stringutils.git $TARGET" )

SConscript( "src/SConscript", variant_dir="build", duplicate=0 )

